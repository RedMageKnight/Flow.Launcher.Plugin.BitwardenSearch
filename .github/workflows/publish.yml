# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 7.0.x
    - name: get version
      id: version
      uses: notiz-dev/github-action-json-property@release
      with: 
        path: 'plugin.json'
        prop_path: 'Version'  
    - name: Restore dependencies
      run: dotnet restore 'Flow.Launcher.Plugin.Bitwarden.csproj'
    - name: Build
      run: dotnet publish 'Flow.Launcher.Plugin.Bitwarden.csproj' -r win-x64  -c Release -o "Bitwarden-${{steps.version.outputs.prop}}"
          7z a -tzip "Bitwarden-${{steps.version.outputs.prop}}.zip" "./Bitwarden-${{steps.version.outputs.prop}}/*"
          rm -r "Bitwarden-${{steps.version.outputs.prop}}"
    - name: Publish
      uses: softprops/action-gh-release@v1
      with:
        files: "Flow.Launcher.Plugin.Bitwarden.zip"
        tag_name: "v${{steps.version.outputs.prop}}"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
